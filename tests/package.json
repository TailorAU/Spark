{
  "name": "spark-e2e",
  "private": true,
  "type": "module",
  "description": "Spark E2E suite (Playwright). Requires SPARK_VAULT_PW in the environment - never stored in the repo.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
