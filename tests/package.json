{
  "name": "tailor-education-family-e2e",
  "private": true,
  "type": "module",
  "description": "Tailor Education family PWA E2E suite. Secret-gated tests require SPARK_VAULT_PW, which is never stored in the repo.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
